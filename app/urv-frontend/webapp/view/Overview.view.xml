<mvc:View controllerName="urvfrontend.controller.Overview"
    xmlns:mvc="sap.ui.core.mvc"
    xmlns="sap.m"
        xmlns:core="sap.ui.core">

    <Page id="page" title="URV Application tool">
    <FlexBox direction="Row" alignItems="Center" width="100%" >
        <Input id="UserID" placeholder="User ID" />
        <Select id="select" >
            <items>
                <core:Item key="user" text="Search by UserID" />
                <core:Item key="group" text="Search by group" />
                
            </items>
        </Select>
    </FlexBox>
    <Button id="userButton" text="Search" type="Emphasized" press="getUser"></Button>


<Panel id="byUserId" visible="false">
    <Panel id="userdetails">
        <content>
            <VBox class="sapUiSmallMargin">
                <Text text="User ID: {userModel>/urn:ietf:params:scim:schemas:extension:sap:2.0:User/id}" />
                <Text text="Name: {userModel>/name/givenName} {userModel>/name/familyName}" />
                <Text text="Email: {userModel>/emails/0/value}" />
                <Text text="User Type: {userModel>/userType}" />
                <Text text="Status: {userModel>/userUuid}" />
            </VBox>
        </content>
    </Panel>

    <Panel id="groups">
        <SearchField 
            width="100%" 
            placeholder="Search in groups..." 
            liveChange=".onSearch" />
        <Tree id="RoleTree" items="{TreeModel>/tree}" mode="None">
            <StandardTreeItem title="{TreeModel>name}" icon="{TreeModel>icon}" />
        </Tree>
    </Panel>
</Panel>


<Panel id="bygroup" visible="false">
    
</Panel>
    
    </Page>
    
</mvc:View>